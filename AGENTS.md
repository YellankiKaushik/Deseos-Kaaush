# Repository Instructions

- Do not rewrite pushed Git history.
- Use npm and `package-lock.json` as the canonical package manager state.
- Keep `.env`, `.env.local`, generated build output, and Supabase temp files out of source control.
- Add schema changes as new Supabase migration files.
- Run typecheck, tests, lint, build, and audit before preparing a pull request.
